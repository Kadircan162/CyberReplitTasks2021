package day41_abstract_class;

interface NewInterface1 { //An interface can be default. And same rules apply that it can only be accessed from the same package, it cannot be accessed from different packages.
}
